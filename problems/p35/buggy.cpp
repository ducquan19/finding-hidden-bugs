#include<bits/stdc++.h>
using namespace std;
long long numOfPairs(int n) {
    long long count=0;
    for (int a = 1; a <= n/2; ++a)
        count += ?;
    return count;
}

int main() {
    int n;
    cin >> n;
    cout << numOfPairs(n) << '\n';
    return 0;
}
